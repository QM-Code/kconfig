# src/game/ui/fonts/AGENTS.md

Read `src/game/ui/AGENTS.md` first.
This directory holds **font loading helpers** for UI frontends.

It defines which font faces are loaded and how fallbacks are configured per
language.
