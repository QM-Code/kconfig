# src/game/ui/frontends/rmlui/console/panels/README.md

RmlUi console panel implementations (`panel_*.cpp`). Each panel maps to a console
TAB and owns its UI logic while using shared controllers/models.
