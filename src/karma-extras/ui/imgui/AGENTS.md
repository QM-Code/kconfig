# src/karma-extras/ui/imgui/AGENTS.md

Read `src/karma-extras/ui/AGENTS.md` first.
This directory contains **ImGui-specific helpers** used by the engine layer.

It primarily exists to support the backend-specific ImGui renderers and bridges.
All game UI logic is in `src/game/ui/frontends/imgui/`.
