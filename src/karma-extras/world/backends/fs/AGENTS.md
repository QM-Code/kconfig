# src/karma-extras/world/backends/fs/AGENTS.md

Read `src/karma-extras/world/AGENTS.md` first.
This directory implements the filesystem world backend.

It resolves world content under the current data root and exposes it via engine
content APIs.
