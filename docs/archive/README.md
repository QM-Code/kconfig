# docs/archive

This folder preserves historical documentation snapshots that were superseded by canonical project docs in `docs/projects/`.

Rules:
- Do not use archive files as primary assignment docs.
- Use `AGENTS.md` -> `docs/AGENTS.md` -> `docs/projects/<project>.md` for active work.
- Archive files exist to avoid information loss during normalization and refactors.
